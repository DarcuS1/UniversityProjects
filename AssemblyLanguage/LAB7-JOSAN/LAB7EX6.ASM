DATA SEGMENT PARA PUBLIC 'DATA'
NUMBERS DB 10 DUP(?)
DATA ENDS

CODE SEGMENT PARA PUBLIC 'CODE'

START PROC FAR

ASSUME CS:CODE, DS:DATA
PUSH DS ;prepare to return to DOS
XOR AX,AX
PUSH AX
MOV AX,DATA; initialise DS
MOV DS, AX


MOV BL, 10
XOR DX, DX

L: 
MOV AH, 01H
INT 21H
;intake
CMP AL, 0DH
JE NEXT
;substitutions
SUB AL, '0'
MOV CL, AL

XOR CH, CH

MOV AX, DX
MUL BL;making the nr as in example
ADD AX, CX
MOV DX, AX

JMP L

NEXT:

RET

START ENDP
CODE ENDS
END START